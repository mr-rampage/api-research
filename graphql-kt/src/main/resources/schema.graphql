type Movie {
    title: String
    year: Int
    imdbRating: Float
    genres: [Genre]
}

type Genre {
    name: String
    movies: [Movie]
}

type Query {
  allMovies: [Movie]
  allGenres: [Genre]
}
